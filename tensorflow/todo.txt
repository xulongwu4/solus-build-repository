--config=mkl

rundeps:
#astor
tensorboard
gast
# protobuf
grpcio
# absl-py
# markdown
# bleach
# werkzeug
# html5lib

#bazel build -c opt --copt=-mavx --copt=-msse4.2 --copt=-msse4.1 --copt=-msse3 --copt=-mavx2 --cop=-mfma --copt=-mfpmath=both -k//tensorflow/tools/pip_package:build_pip_package
bazel build -c opt --copt=-mavx --copt=-msse4.2 --copt=-msse4.1 --copt=-msse3 --copt=-mavx2 --cop=-mfma -k//tensorflow/tools/pip_package:build_pip_package

tensorboard
c api
go api


X ** Do not use wheel file, use python setup.py build and install
X ** Remove "PYTHON_BIN_PATH" if it is only used when building pip file?
X    - Yes it works.
X    - Need to 
X        1) Comment out the function setup_python
X        2) In build_pip_package.sh, need to comment out building wheel part
            and only copy the contents to the destination directory. Also comment out the
            line of source tools/python_bin_path.sh (it only contains one line: 'export
            PYTHON_BIN_PATH="/usr/bin/python3"' before the patch. It does not exist after
            we apply the patch)
X ** Remove the external libraries (OpenSUSE build)
** Do I need libtensorflow.so and other libraries for c api (see arch build)?
** Build tensorboard
